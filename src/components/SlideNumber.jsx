export default function SlideNumber({ label }) {
  return (
    <div className="font-mono text-[0.7rem] font-medium uppercase tracking-[0.2em] text-accent mb-6 opacity-70">
      {label}
    </div>
  )
}
